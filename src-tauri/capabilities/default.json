{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "core:window:allow-open-devtools",
    "core:window:allow-close-devtools",
    "core:window:allow-is-devtools-open"
  ]
}
